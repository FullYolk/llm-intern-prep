## git
git add .
git commit -m ""
git push
git branch
git switch -c ****
git switch main
git merge ****
git restore **(文件名) 适用于尚未add
git log --oneline
git reset soft abcd1234(已commit 尚未push)
git revert bad5678（已push）'
git pull
## linux
pwd
ls
ls -la
cd ..
cd -
mkdir temp_test
rm -r temp_test
cp README.md README_copy.md
mv README_copy.md assets/
cat README.md
find . -name "*.md"
find . -iname -type
find . -size +100M
find . -mtime -1
grep -R "目标" .
grep -i(忽略大小写)
grep -rn(递归查找所有子目录 显示在第几行)
grep -v(反向选择)
grep -C(显示匹配行的前后三行)
